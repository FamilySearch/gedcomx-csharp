using Newtonsoft.Json.Linq;
// <auto-generated>
// 
//
// Generated by <a href="http://enunciate.codehaus.org">Enunciate</a>.
// </auto-generated>
using System;
using System.Collections.Generic;
using System.Linq;

namespace Gx.Common
{

    /// <remarks>
    ///  A set of data that supports extension elements.
    /// </remarks>
    /// <summary>
    ///  A set of data that supports extension elements.
    /// </summary>
    [System.SerializableAttribute()]
    [System.Xml.Serialization.XmlTypeAttribute(Namespace = "http://gedcomx.org/v1/", TypeName = "ExtensibleData")]
    public abstract partial class ExtensibleData
    {
        private string _id;
        private List<Object> _extensionElementsXml;
        private Dictionary<String, JToken> _extensionElementsJson;

        /// <summary>
        ///  A local, context-specific id for the data.
        /// </summary>
        [System.Xml.Serialization.XmlAttributeAttribute(AttributeName = "id", DataType = "ID")]
        [Newtonsoft.Json.JsonProperty("id")]
        public string Id
        {
            get
            {
                return this._id;
            }
            set
            {
                this._id = value;
            }
        }

        /// <summary>
        ///  Custom extension elements for a conclusion.
        /// </summary>
        [System.Xml.Serialization.XmlAnyElementAttribute()]
        [Newtonsoft.Json.JsonIgnore]
        public List<Object> ExtensionElementsXml
        {
            get
            {
                return this._extensionElementsXml;
            }
            set
            {
                this._extensionElementsXml = value;
            }
        }

        /// <summary>
        ///  Custom extension elements for a conclusion.
        /// </summary>
        [System.Xml.Serialization.XmlIgnore]
        [Newtonsoft.Json.JsonExtensionData]
        public Dictionary<String, JToken> ExtensionElementsJson
        {
            get
            {
                return this._extensionElementsJson;
            }
            set
            {
                this._extensionElementsJson = value;
            }
        }

        protected internal virtual void Embed(ExtensibleData data)
        {
            if (data._extensionElementsXml != null)
            {
                this._extensionElementsXml = this._extensionElementsXml == null ? new List<Object>() : this._extensionElementsXml;
                this._extensionElementsXml.AddRange(data._extensionElementsXml);
            }

            if (data._extensionElementsJson != null)
            {
                this._extensionElementsJson = this._extensionElementsJson == null ? new Dictionary<String, JToken>() : this._extensionElementsJson;

                foreach (var element in data._extensionElementsJson)
                {
                    this._extensionElementsJson.Add(element.Key, element.Value);
                }
            }
        }

        /**
         * Find the extensions of a specified type.
         *
         * @param clazz The type.
         * @return The extensions, possibly empty but not null.
         */
        public List<T> FindExtensionsOfType<T>(params String[] jsonElementNames)
        {
            List<T> ext = new List<T>();
            if (this._extensionElementsXml != null)
            {
                foreach (Object extension in _extensionElementsXml)
                {
                    if (typeof(T).IsAssignableFrom(extension.GetType()))
                    {
                        ext.Add((T)extension);
                    }
                }
            }

            if (this._extensionElementsJson != null)
            {
                foreach (var extension in _extensionElementsJson.Where(x => jsonElementNames.Contains(x.Key)).Select(x => x.Value))
                {
                    if (extension.Type == JTokenType.Array)
                    {
                        foreach (var item in (JArray)extension)
                        {
                            ext.Add(item.ToObject<T>());
                        }
                    }
                    else
                    {
                        ext.Add(extension.ToObject<T>());
                    }
                }
            }

            return ext;
        }

        /**
         * Add an extension element.
         *
         * @param element The extension element to add.
         */
        public void AddExtensionElement(Object element, String jsonElementName = null, bool forceArray = false)
        {
            if (this._extensionElementsXml == null)
            {
                this._extensionElementsXml = new List<Object>();
            }

            if (this._extensionElementsJson == null)
            {
                this._extensionElementsJson = new Dictionary<string, JToken>();
            }

            if (element != null)
            {
                var elementName = jsonElementName ?? element.GetType().Name;
                this._extensionElementsXml.Add(element);

                if (this._extensionElementsJson.ContainsKey(elementName))
                {
                    var existing = this._extensionElementsJson[elementName].ToList();
                    existing.Add(JToken.FromObject(element));
                    this._extensionElementsJson[elementName] = new JArray(existing.ToArray());
                }
                else
                {
                    if (forceArray)
                    {
                        this._extensionElementsJson.Add(elementName, JToken.FromObject(new Object[] { element }));
                    }
                    else
                    {
                        this._extensionElementsJson.Add(elementName, JToken.FromObject(element));
                    }
                }
            }
        }

        /**
         * Build up this object with an id.
         *
         * @param id The id.
         */
        public ExtensibleData SetId(String id)
        {
            this.Id = id;
            return this;
        }

    }
}
